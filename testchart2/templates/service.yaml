apiVersion: v1
kind: Service
metadata:
  name: {{ include "testchart2.fullname" . }}
  labels:
    {{- include "testchart2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testchart2.selectorLabels" . | nindent 4 }}
